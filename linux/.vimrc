" set syntax always on
syntax on

" COLOR
" set scheme and background
colorscheme desert
set background=dark

" ENCODING
" set utf8 as the standard encoding
set encoding=utf8

" TABS
" use spaces instead of tabs
set expandtab
set smarttab

" show whitespaces
set list

" tabwidth
set shiftwidth=4
set tabstop=1
